# Brain config, dialog credentials, and per-golem Spirit data all live in
# data/forge.json — edit them in the forge UI (uv run golem forge), not here.

# Optional: bind the forge UI to a non-default host/port.
# FORGE_HOST=127.0.0.1
# FORGE_PORT=8765
